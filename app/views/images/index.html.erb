<div class="containner banner">
<h1 class="text-center">Welcome to Nice Messes</h1>
</div>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-12 col-sm-4 journey-form">
      <h3>Plan a journey</h3>
        <%= simple_form_for @itinerary do |f| %>
          <%= f.input :start_address, placeholder: "From" %>
          <%= f.input :end_address, placeholder: "To" %>
          <%= f.input :mode, collection: [["Voiture", 0], ["Vélo", 1], ["Marche", 2]],  prompt: "Select your transportation method" %>
          <%= f.button :submit, class: "btn btn-outline-secondary" %>
        <% end %>
    </div>
  </div>
</div>
<div class="container">
  <ul>
    <% @images.each do |i| %>
      <li><%= i %></li>
    <% end %>
  </ul>
</div>
